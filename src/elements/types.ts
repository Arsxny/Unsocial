export type TabProps = {
    children: React.ReactNode;
    index: number;
    value: number;
}

export type TabComponentType = {
    headerHeight: number;
}

export type SideBarType = {
    headerHeight: number;
}